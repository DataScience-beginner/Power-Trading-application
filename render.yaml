services:
  - name: power-trading-backend
    type: web
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "uvicorn backend.main:app --host 0.0.0.0 --port 8000"
    envVars:
      - key: ALLOWED_ORIGINS
        value: "*"