# Release Management Memory

- Production branch is currently `V2`; rename to `main` only through a planned migration.
- The release path is `develop` → `staging` → `V2`.
- Production requires cofounder approval.
