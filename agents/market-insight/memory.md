# Market Insight Memory

## AI-1 baseline

- Deterministic intent routing supports data quality, coverage, recorded cost, and schedule-data questions.
- Market explanations aggregate current V0 files and transactions and record AI-0 audit/decision evidence.
- No external LLM provider is configured. A future provider may narrate structured evidence but cannot replace deterministic calculations.
