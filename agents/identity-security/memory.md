# Identity Security Agent Memory

- Identity V1 separates administrator and client portals while retaining one server-side identity service.
- SMTP email and SMS webhook adapters fail closed when environment configuration is absent.
- Passkeys/TOTP and managed enterprise SSO remain the next authenticator milestone.
