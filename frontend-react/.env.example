# Production uses same-origin API through /api by default.
# For local dev, uncomment and point to your FastAPI backend:
# VITE_API_URL=http://localhost:8000/api
VITE_AUTO_LOGOUT_ENABLED=true
VITE_IDLE_TIMEOUT_MINUTES=30
VITE_COOKIE_AUTH_ONLY=true
