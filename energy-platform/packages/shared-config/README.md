# Shared Config

Shared settings package placeholder.

Use this folder later for:

- common tenant config schemas
- feature flags
- service config loaders
- mapping templates for different client workbook formats
